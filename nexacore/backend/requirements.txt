# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Database (SQLAlchemy ORM + Alembic migrations + Postgres driver) ---
sqlalchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9

# --- Validation / settings ---
pydantic==2.9.2
email-validator==2.2.0
pydantic-settings==2.5.2

# --- Authentication (JWT + password hashing) ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- File handling (multipart uploads, CSV/Excel import, PDF export) ---
python-multipart==0.0.9
pandas==2.2.2
openpyxl==3.1.5
reportlab==4.2.2

# --- Rate limiting ---
slowapi==0.1.9

# --- AI assistant (optional at runtime; only used if ANTHROPIC_API_KEY is set) ---
anthropic==0.34.2

# --- Testing ---
pytest==8.3.2
pytest-cov==5.0.0
httpx==0.27.2
