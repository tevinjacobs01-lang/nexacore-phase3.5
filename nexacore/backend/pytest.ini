[pytest]
testpaths = app/tests
python_files = test_*.py
addopts = -v --tb=short

; Fixture note: several older test files (test_importer.py, test_leads.py,
; etc.) define their own local `db` fixture identical in behavior to the
; one in conftest.py. Pytest fixture resolution means a test file's own
; fixture takes precedence over conftest.py's, so this is harmless
; duplication rather than a conflict — left as-is per the instruction not
; to refactor working code during this hardening pass.
